server {
    listen 80;
    root /usr/share/nginx/html;
    index landing.html;

    location = / {
        try_files /landing.html =404;
    }

    location /app {
        try_files $uri $uri/ /index.html;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
